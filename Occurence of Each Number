vector<int> Solution::findOccurences(vector<int> &A){
 map<int,int> m;
 //sort(A.begin(),A.end());
 for(int i=0;i<A.size();i++){
      m[A[i]]++;
}
vector<int> res;
 for(auto x:m){
     res.push_back(x.second);
 }
  return res;
}
